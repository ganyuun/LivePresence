[project]
name = "livepresence"
version = "0.1.0"
description = "Discord RPC for the web!"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncio>=4.0.0",
    "dataclasses>=0.8",
    "discord-rpc>=5.5",
    "keyring>=25.7.0",
    "nicegui>=3.12.1",
    "websockets>=16.0",
]
